summary: Register new user
operationId: register-users
description: Register a user
tags:
  - auth
requestBody:
  content:
    application/json:
      schema:
        $ref: "../../schemas/register.yaml"
responses:
  200:
    description: user register with success
    content:
      application/json:
        schema:
          $ref: "../../schemas/register-response.yaml"
  201:
    $ref : "../../common/responses/null-response.yaml"
  default:
    $ref : "../../common/responses/unexpected-error.yaml"