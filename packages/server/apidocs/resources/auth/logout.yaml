summary: Logout new user
operationId: logout-users
description: Logout a user
tags:
  - auth
requestBody:
  content:
    application/json:
      schema:
        $ref: "../../schemas/logout.yaml"
responses:
  200:
    description: user logout with success
    content:
      application/json:
        schema:
          $ref: "../../schemas/logout-response.yaml"
  201:
    $ref : "../../common/responses/null-response.yaml"
  default:
    $ref : "../../common/responses/unexpected-error.yaml"