summary: Create
operationId: create-scopes
description: Create a scope
tags:
  - scopes
requestBody:
  content:
    application/json:
      schema:
        $ref: "../../schemas/scope.yaml"
responses:
  201:
    $ref : "../../common/responses/null-response.yaml"
  default:
    $ref : "../../common/responses/unexpected-error.yaml"