const CREDENTIAL_USE_CASE = {
  INVALID_PHONE_NUMBER: 'INVALID_PHONE_NUMBER',
  INVALID_CREDENTIALS: 'INVALID_CREDENTIALS',
};

const CREDENTIAL_USE_CASES = Object.values(CREDENTIAL_USE_CASE);

module.exports = { CREDENTIAL_USE_CASE, CREDENTIAL_USE_CASES };
