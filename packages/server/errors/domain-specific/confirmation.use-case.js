const CONFIRMATION_USE_CASE = {
  NOT_CONFIRMED_EMAIL_ALREADY_TAKEN: 'NOT_CONFIRMED_EMAIL_ALREADY_TAKEN',
  NOT_CONFIRMED_FULLNAME_ALREADY_TAKEN: 'NOT_CONFIRMED_FULLNAME_ALREADY_TAKEN',
  NOT_CONFIRMED_PHONE_NUMBER_ALREADY_TAKEN:
    'NOT_CONFIRMED_PHONE_NUMBER_ALREADY_TAKEN',
};

const CONFIRMATION_USE_CASES = Object.values(CONFIRMATION_USE_CASE);

module.exports = { CONFIRMATION_USE_CASE, CONFIRMATION_USE_CASES };
